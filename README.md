# Api Server Basico con DENO

### Para usar en local ejecutar:

``` deno run --allow-net server.ts ``` --- o --- ``` denon run --allow-net server.ts ```

### Para probar, usar Thunder Client exportando: ``` collection_deno-sample.json ```